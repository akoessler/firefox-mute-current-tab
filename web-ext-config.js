module.exports = {
    // Global options:
    verbose: true,
    sourceDir: "v2",
    ignoreFiles: [
        "**/*.ps1",
        "**/*.svg"
    ],
    // Command options:
    build: {
        overwriteDest: true
    }
};
